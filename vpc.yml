##################################################################################
##      CONFIGURE AWS VPC
##################################################################################

include: vpcbuild.yml
with_items:  "{{ job_vars }}"
when:
  - item.job_tag == 'vpcbuild'