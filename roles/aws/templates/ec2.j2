# Create an EC2 Instance
resource "aws_instance" "example" {
  ami                    = "ami-0c55b469f13175b49" # Replace with a valid AMI ID for your region
  instance_type          = "t2.micro"             
  tags = {
    Name = "{{ item.ec2Name }}"
  }
}