---

- include: vpcbuild.yml
  with_items: "{{ job_vars }}"
  when: 
  
    - item.tags == "aws_vpc"


- include: user.yml
  with_items: "{{ job_vars }}"
  when: 
  
    - item.tags == "aws_user"

- include: provider.yml

